# Production Environment — Node API
# These values should be overridden via Docker Compose or K8s environment variables

PORT=3002
JWT_SECRET=
JWT_EXPIRATION=3600
AUTH_USERNAME=admin
AUTH_PASSWORD=
NODE_ENV=production
CORS_ORIGIN=*
